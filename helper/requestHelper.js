const request = require('supertest');
request;
const app = require('../app');
app;
module.exports = {
  request: ({ method, endPoint, statusCode }) => {
    method;
    endPoint;
    statusCode;
  },
};
